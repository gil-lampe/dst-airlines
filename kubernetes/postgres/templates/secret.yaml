apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  postgresPassword: {{ .Values.postgresql.secrets.postgresPassword }}
  postgresUser: {{ .Values.postgresql.secrets.postgresUser }}
---